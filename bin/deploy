#!/bin/bash

if [ ! -d ~/tmp ]; then
	mkdir ~/tmp/
fi

JAR="TicTacToe-SoftwareBoys.jar"

bin/clean
bin/build
bin/package

test -f /tmp/${JAR} && rm /tmp/${JAR}
cp build/libs/${JAR} ~/tmp

java -jar ~/tmp/${JAR}
