#!/bin/bash
if [ ! -d ~/tmp ]; then
        mkdir ~/tmp/
        mkdir ~/tmp/DOCUMENTS
        mkdir ~/tmp/PACKAGES
        mkdir ~/tmp/TESTREPORT
fi

./gradlew test


cp -a build/reports/tests/. ~/tmp/TESTREPORT
